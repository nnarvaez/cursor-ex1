# cursor-ex1 🎨

Proyecto de práctica en Python para familiarizarse con Cursor, Git y pruebas unitarias.

## Contenido
- Scripts simples (`holamundo.py`, `fizzbuzz.py`, `calculadora.py`)
- Archivos de texto de ejemplo como `mitexto.txt`
- Tests básicos en `test_*.py`

## Requisitos
- Python 3.13 (incluido en el entorno `cursorintro/`)
- Dependencias listadas en el virtualenv (autopep8, flake8, etc.)

## Cómo usar
```bash
pwsh -ExecutionPolicy Bypass -File cursorintro/Scripts/Activate.ps1
python holamundo.py
pytest
```

## Contribuir
1. Crea una rama nueva
2. Haz tus cambios y corre los tests
3. Haz pull request a `main`